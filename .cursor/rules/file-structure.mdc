---
alwaysApply: true
---

# Component Organization

## Reusable Components
- **Location**: `app/components/`
- Used across multiple routes/pages
- Examples: `TextInput`, `NumberInput`, `DateInput`, `Button`, `Card`
- Export from `app/components/index.ts`

### Reusable component with colocated files

When a reusable component has **one or more** related modules (custom hooks, styles-only files, small helpers used only by that component), **group them in a folder** named like the component:

- **Location**: `app/components/[ComponentName]/`
- **`[ComponentName].tsx`** — main component (PascalCase, matches folder)
- **No `index.ts` barrel inside the folder** unless there is a clear need; re-export from `app/components/index.ts` using explicit paths (e.g. `export { Dropdown } from './Dropdown/Dropdown'`), or import from `@/app/components/Dropdown/Dropdown` where appropriate.
- **Hooks** used only by this component live next to it, e.g. `useDropdownOutsideDismiss.ts` (use a **dropdown-prefixed** name when the hook is specific to that component’s behavior — not a generic “click outside” name). Route-local hooks stay in `app/[route]/_hooks/`, not here.
- **Example:**
```
app/components/Dropdown/
├── Dropdown.tsx
└── useDropdownOutsideDismiss.ts
```

## Page-Specific Components
- **Location**: `app/_components/`
- Specific to a single page/route
- Examples: `DividendsInput`, `BondForm`, `ResultsDisplay`
- Underscore prefix (`_`) indicates private/internal (not a route)

## When to Extract

**Reusable (`app/components/`):**
- Used in 2+ places
- UI primitive (input, button, card)
- No domain-specific business logic

**Page-Specific (`app/_components/`):**
- Single page/route usage
- Domain-specific logic
- Extracted for readability/single responsibility

## Guidelines
1. Single responsibility per component
2. Start in `_components/`, move to `components/` if reused
3. PascalCase naming
4. TypeScript types required
5. Separate reusable UI from business logic

## Component Naming Convention

**Feature Prefix Pattern:**
When a component is part of a larger feature/component hierarchy, prefix all components in that hierarchy with the feature name. Build up the prefix as you go deeper in the component tree.

**Apply prefix to:**
- All components that are part of a feature's component tree
- Components used directly by the parent feature component
- Components nested within feature components (maintain the prefix chain)

**Prefix rules:**
1. Start with the feature name (e.g., `Results`)
2. For nested components, append the parent component name (e.g., `ResultsBondCard`)
3. Continue building the prefix for deeper nesting (e.g., `ResultsBondBasicParams`)

**Don't prefix:**
- Truly reusable UI primitives in `app/components/` (used across features)
- Components that are standalone and not part of a feature hierarchy

**Example:**
```
_components/
├── Results.tsx                           # Main feature component
├── ResultsSummary.tsx                    # ✓ Prefixed - used by Results
├── ResultsBondCard.tsx                   # ✓ Prefixed - used by Results
├── ResultsBestOptionCard.tsx             # ✓ Prefixed - used by ResultsSummary
├── ResultsComparison.tsx                 # ✓ Prefixed - used by ResultsSummary
├── ResultsBondBasicParams.tsx            # ✓ Prefixed - nested in ResultsBondCard
├── ResultsBondDividends.tsx              # ✓ Prefixed - nested in ResultsBondCard
└── ResultsBondReturnCalculation.tsx      # ✓ Prefixed - nested in ResultsBondCard
```

**Component Hierarchy:**
```
Results (main)
├── ResultsBondCard
│   ├── ResultsBondBasicParams
│   ├── ResultsBondDividends
│   └── ResultsBondReturnCalculation
└── ResultsSummary
    ├── ResultsBestOptionCard
    └── ResultsComparison
```

**Benefits:**
- Groups related files together in file listings (all Results components appear together)
- Makes component ownership and hierarchy clear
- Prevents naming conflicts across features
- Improves discoverability and navigation

```
app/
├── components/        # Reusable (across routes)
│   ├── ui/           # UI primitives (optional)
│   ├── TextInput.tsx
│   ├── Dropdown/     # folder when component + hooks/helpers
│   └── index.ts
└── _components/      # Page-specific (private)
    └── DividendsInput.tsx
```

---

# Page Organization

## Special Files

**Required:**
- `page.tsx` - Route UI (creates public route)
- `layout.tsx` - Shared UI wrapper

**Optional:**
- `loading.tsx` - Loading state
- `error.tsx` - Error boundary
- `not-found.tsx` - 404 page
- `template.tsx` - Re-rendered layout

## File Naming

1. **Route files** (lowercase): `page.tsx`, `layout.tsx`, `loading.tsx`, `error.tsx`, `not-found.tsx`
2. **Client components** (kebab-case): `*-client.tsx` suffix (e.g., `page-client.tsx`)
3. **Private folders** (underscore): `_components/`, `_hooks/`, `_lib/`, `_types/`

## Custom hooks (local vs global)

**Route-local hooks** — used only by one route’s `page-client` (or components under that route):

- **Location**: `app/[route]/_hooks/` in the **same route segment** as `page.tsx` / `page-client.tsx`
- Example: `app/_hooks/useBondInclusion.ts` for `app/page-client.tsx` (home)
- Nested example: `app/results/_hooks/useSomething.ts` for `app/results/page-client.tsx`
- File naming: `use*.ts` (e.g. `useBondInclusion.ts`)
- Do **not** park single-route hooks in a generic `app/_lib/` at the app root; `_hooks/` next to the page makes ownership obvious

**Shared / global hooks** — reused across unrelated routes:

- **Location**: a single project-wide place (e.g. `hooks/` at repo root, or `lib/hooks/`, or `app/hooks/`) — pick one convention
- Extract only when a second consumer appears

**`_lib/`** — route-local **non-hook** utilities (plain functions, small modules), shared types, etc. Not the default home for React hooks.

## Route Decisions

**Create new route when:**
- Distinct navigation/screen
- Needs own URL path
- Benefits from code splitting
- Needs separate layout/metadata

**Use route groups `(group)` when:**
- Organizing without URL changes
- Sharing layouts across routes

**Keep in same route when:**
- Modal/drawer
- Tab/panel within page
- No URL needed

## Client vs Server Components

**Server (default):** `page.tsx`, `layout.tsx` - Data fetching, static content, SEO

**Client:** `*-client.tsx` with `'use client'` - Interactivity, hooks, browser APIs

**Pattern:**
```tsx
// app/page.tsx (Server)
import ClientComponent from './page-client';
export default function Page() {
  return <ClientComponent />;
}

// app/page-client.tsx (Client)
'use client';
export default function ClientComponent() {
  // Interactive logic
}
```

## Guidelines
1. Start simple, extract routes as needed
2. Use parentheses `(group)` for organization without URL changes
3. Nested layouts create UI hierarchy
4. Add `'use client'` only when necessary
5. Each route auto code-splits
6. Add metadata in `page.tsx` or `layout.tsx`
7. Use `loading.tsx` and `error.tsx` for better UX

## Complete Structure

```
app/
├── layout.tsx                    # Root layout
├── page.tsx                      # Home (/)
├── page-client.tsx               # Client logic
├── globals.css
├── components/                   # Reusable
│   ├── ui/
│   ├── TextInput.tsx
│   ├── Dropdown/
│   │   ├── Dropdown.tsx
│   │   └── useDropdownOutsideDismiss.ts
│   └── index.ts
├── _components/                  # Page-specific
│   └── DividendsInput.tsx
├── _hooks/                       # Page-local hooks (same route as page-client)
│   └── useBondInclusion.ts
├── about/
│   ├── page.tsx                  # /about
│   └── _components/
├── dashboard/
│   ├── layout.tsx
│   ├── page.tsx                  # /dashboard
│   ├── page-client.tsx
│   ├── loading.tsx
│   ├── error.tsx
│   ├── _components/
│   ├── _hooks/
│   ├── _lib/
│   └── settings/
│       └── page.tsx              # /dashboard/settings
└── (auth)/                       # Route group
    ├── layout.tsx
    ├── login/page.tsx            # /login
    └── register/page.tsx         # /register
```

## Route Template

```
app/[route-name]/
├── page.tsx              # Required
├── page-client.tsx       # Optional
├── layout.tsx            # Optional
├── loading.tsx           # Optional
├── error.tsx             # Optional
├── not-found.tsx         # Optional
├── _components/          # Optional
├── _hooks/               # Optional — hooks for this route only
└── _lib/                 # Optional — non-hook utilities for this route
```
